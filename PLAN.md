# 1歳向け赤いボール探し・タップゲーム 企画・実装プラン

## Context（背景・目的）

1歳の子供がiPadで遊べるシンプルなタッチゲームを作る。
赤いボールを「かくれんぼ→逃げる→捕まえる」という3フェーズで楽しめるゲーム。
大きな視覚フィードバックと音で、まだ言語を持たない乳幼児でも直感的に遊べることを目指す。

---

## 1歳向けゲームデザイン原則（重要）

### 1歳児の特性
- 指先の精度が低い → **タップ対象は画面の20〜30%を占めるほど大きく**
- 注意持続時間は30秒〜2分程度 → **1ゲーム20〜30秒以内**
- 成功体験が重要 → **失敗=ゲームオーバーは設けない**
- 音と動きに強く反応する → **音・アニメーションは派手に**
- マルチタッチの誤操作 → **画面全体のタップをうまく処理**

### アドバイス：企画への追加提案
1. **ヒント演出を入れる**：隠れている間も少し揺れる/チラ見えする（子供が探す手がかり）
2. **リトライしやすく**：クリア後すぐに次のかくれんぼが始まる（ループ設計）
3. **難易度の自動調整**：タップしないと一定時間後にボールが自分で出てくる（フラストレーション防止）
4. **効果音の選定**：笑い声、ポップ音、音楽など赤ちゃんが喜ぶ音
5. **背景はシンプル**：情報過多にしない（1歳は刺激に敏感）

---

## ゲームフロー（状態機械）

```
[HIDE フェーズ]
  ボールが草むらの後ろに隠れる
  ↓ 少し揺れたり、チラ見えする（ヒント）
  ↓ ユーザーが草むらをタップ OR 5秒後自動

[APPEAR フェーズ]
  「ばあ！」的な演出でボール出現
  効果音：ポップ音
  ↓ 0.5秒後

[RUN フェーズ]
  ボールが画面内を逃げ回る（ゆっくり）
  効果音：ころころ音
  ↓ ユーザーがボールをタップ OR 10秒後自動で捕まる

[CATCH フェーズ（クリア）]
  派手な演出：紙吹雪、ボールがキラキラ、画面が光る
  効果音：ファンファーレ
  ↓ 3秒後

[HIDE フェーズ] に戻る（ループ）
```

---

## 技術スタック（確定）

- **言語**: HTML5 + CSS3 + Vanilla JavaScript
- **音声**: Web Audio API（外部ファイル不要、プログラムで合成）
- **動作環境**: iPad Safari（追加インストール不要）

### ファイル構成
```
find-the-red-ball/
├── index.html   # メインHTML（iPadフルスクリーン対応）
├── style.css    # スタイル・アニメーション
├── game.js      # ゲームロジック（状態機械）
└── PLAN.md      # 本ファイル
```

---

## 草むら隠れ演出の詳細設計

### 視覚的な実現方法
```
画面下部に草むら（SVG or CSS clip-path で不規則な緑の山）
ボールが草むらの後ろに移動して隠れる

【隠れ中のヒント演出】
・草むらが微妙に揺れる (CSS animation: wiggle)
・草むらから赤い色がチラ見え (ボールの一部だけ見える)
・草むらをタップすると「ばあ！」でボールが飛び出す

【出現演出】
・草むらが左右に開く
・ボールが上にぴょんと飛び出る
```

### 草むら実装
- CSS `clip-path: polygon(...)` で不規則な草の形状
- ボールは草むらの `z-index` の下に配置して隠す

---

## Web Audio API 効果音設計

```javascript
// 出現音：明るいポップ音
playPop()         → OscillatorNode (sine wave, 880Hz→1200Hz, 0.2秒)

// 逃げる音：ころころ音
playRoll()        → OscillatorNode (sawtooth, 440Hz, 断続的)

// 捕まえた音：ファンファーレ
playFanfare()     → 3音和音 (C-E-G, triangle wave, 0.8秒)

// 紙吹雪演出中：明るいメロディー
playCelebration() → 順次和音（ドレミファソ）
```

---

## 実装順序

1. `index.html` 基本構造作成（iPadフルスクリーン対応）
2. `style.css` でベース背景・ボール・草むら要素スタイル
3. `game.js` 状態機械の骨格（HIDE/APPEAR/RUN/CATCH）
4. HIDE フェーズ：ボールを草むらに隠す演出
5. APPEAR フェーズ：タップで草むらが開いてボール出現
6. RUN フェーズ：ボールが逃げる動き（requestAnimationFrame）
7. CATCH フェーズ：当たり判定 + 捕まえる判定（大きめ）
8. クリア演出（紙吹雪パーティクル・画面フラッシュ）
9. 効果音（Web Audio API合成）
10. 動作テスト・調整

---

## 検証方法

1. ブラウザで `index.html` を直接開く
2. Chrome DevTools でiPadサイズ（768×1024）にシミュレート
3. タッチエミュレーションモードで動作確認
4. 実機iPad（Safari）でテスト
5. 1歳児に実際に触らせて反応を確認
